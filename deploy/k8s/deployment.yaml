apiVersion: apps/v1
kind: Deployment
metadata:
  name: blogo
  labels:
    app.kubernetes.io/component: server
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: blogo
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/component: server
    spec:
      imagePullSecrets:
        - name: blogo-pull-secret
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      securityContext:
        # UIDs omitted: OpenShift SCC restricted-v2 assigns from namespace range
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: blogo
          image: quay.io/hclareth/blogo:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: BLOGO_CONFIG
              value: "/config/blogo.yaml"
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop:
                - ALL
          startupProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: content
              mountPath: /content
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: config
          configMap:
            name: blogo-config
        - name: content
          emptyDir: {}
        - name: tmp
          emptyDir: {}
