apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: blogo-tls
spec:
  secretName: blogo-tls
  issuerRef:
    name: ca-cluster-issue-letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - blogo.hclareth.space
